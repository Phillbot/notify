{
  "name": "desktop",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "copy:web": "cpy \"../web/dist/**/*\" public",
    "prestart": "nx build web && pnpm run copy:web",
    "predev": "pnpm run prestart",
    "start": "electron .",
    "dev": "electron .",
    "prebuild": "nx build web && pnpm run copy:web",
    "build:mac": "pnpm run prebuild && electron-builder --mac",
    "build:win": "pnpm run prebuild && electron-builder --win",
    "build:linux": "pnpm run prebuild && electron-builder --linux"
  },
  "build": {
    "appId": "com.notify.desktop",
    "mac": {
      "target": "dmg"
    },
    "win": {
      "target": "nsis"
    },
    "linux": {
      "target": "AppImage"
    },
    "directories": {
      "output": "release"
    },
    "files": [
      "index.js",
      "public/**/*"
    ]
  },
  "dependencies": {
    "web": "workspace:*"
  },
  "devDependencies": {
    "electron-builder": "catalog:desktop",
    "electron": "catalog:desktop",
    "cpy-cli": "catalog:"
  }
}
